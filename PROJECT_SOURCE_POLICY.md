# 项目源码保留规范

本仓库用于管理 iOS 插件、越狱 tweak、App 相关项目。自本规范生效起，仓库内每个插件或 App 目录都必须保留对应源码，不能只保留编译产物。

## 适用范围

- `plugins/<name>/`
- `app/<name>/`
- 以及后续新增的同类项目目录

## 强制规则

1. 每个插件或 App 目录下，必须有可维护的源码文件。
2. 不允许只保留 `.deb`、`.theos/`、二进制、设备导出文件，而没有正式源码。
3. 后续所有功能修改、Bug 修复、行为变更，都必须同步修改并保留对应源码。
4. 已确认可用的正式源码，不允许随意删除。
5. 如果某次写错了、试验失败了、生成了明显错误的临时文件，可以删除这些错误文件，但不能把可维护的正式源码一起删掉。
6. 如果源码来自反编译、设备导出、包内恢复或人工重建，也必须放回对应项目目录内，并明确标注来源或恢复说明，保证下次还能继续改。
7. 一个项目目录应优先包含以下内容中的合理子集：
   - `Tweak.xm` / `.xm` / `.m` / `.mm` / `.swift`
   - `Makefile`
   - `control`
   - 注入或配置相关的 `.plist`
   - `Preferences/` 或其他源码子目录
   - 必要的 `README.md`
8. 如果项目包含 `Preferences/`，其中的源码、配置、资源和展示内容必须与该项目当前最新版插件保持一致；插件功能、选项或版本发生变化时，必须同步更新 `Preferences/`，不得保留与最新版不匹配的旧内容。

## 推荐目录原则

1. 源码和对应插件必须放在同一个项目目录下，不要把源码散落到仓库外或别的临时目录。
2. 构建产物可以保留，但构建产物不能代替源码。
3. `.theos/` 只是构建输出，不算源码。
4. 所有 `.deb` 必须统一存放在对应项目的 `packages/` 目录中，不得散落在项目根目录；安装包只是发布产物，不算源码。

## 安全与性能要求

1. 每次变更必须限制在实现目标所需的最小范围内，不得顺带修改无关功能、系统行为或公共组件。
2. 插件只能影响其明确声明的目标功能，不得破坏或干扰系统原有功能、稳定性、安全性及其他插件的正常运行。
3. 不得引入持续高 CPU 占用、无必要的高频轮询、忙等待、重复计算或长期运行的后台任务；能够使用系统事件、通知或回调时，应优先采用事件驱动方式。
4. 不得造成可感知的额外耗电、发热或不合理的内存、线程、文件句柄及网络资源占用。
5. 新增的监听、定时器、线程、任务和其他资源必须具有明确的生命周期，并在不再需要时及时停止或释放。
6. 修改完成后必须检查变更范围及潜在的系统稳定性、CPU、内存和续航影响；无法完成实机验证时，必须明确说明未验证项和潜在风险，不得将其描述为已验证通过。

## 变更要求

1. 修改某个插件时，优先改该插件目录中的正式源码。
2. 如果发现目录里只有产物、没有源码，先补齐源码，再继续改功能。
3. 提交时，源码变更应作为主内容；产物是否同时提交，按仓库当前策略处理，但不能只提交产物不提交源码。

## 目的

这份规范的目标只有一个：让仓库里的每个插件和 App 都能持续维护，下一次修改时能直接在对应目录里找到源码，而不是重新从包、设备或二进制里恢复。
