HideDoubaoPiP v1.0.23

主要作用：基于 1.0.22 的成功方案做 release 收敛，保留豆包 PiP 抢占能力并降低日志量。

主要变化：
- 保持 `SBPictureInPictureWindow` 可见、不禁用交互、不调用 stash、不移动 frame。
- 保持 `PGHitTestExtendableView` 作为系统可见的命中容器，只隐藏其 CALayer 渲染，并隐藏内部内容/控制层。
- 删除高频 `[CALL]`、`[MISS]`、`[SETALPHA-PASS]` 测试日志，只保留 `[INIT]` 和低频/状态变化 `[HIDE]` 日志。
- 日志文件上限从 512KB 降到 256KB，减少长期测试时的磁盘写入。

HideDoubaoPiP v1.0.22

主要作用：修正 1.0.21 隐藏 `PGHitTestExtendableView.alpha` 后仍无法挤占微信 PiP 的问题。

主要变化：
- 保留 `PGHitTestExtendableView` 的 UIView `alpha=1` 和交互状态，只将其 CALayer `opacity=0` 用于视觉隐藏。
- 继续隐藏内部内容/控制层，但不改 `SBPictureInPictureWindow`、不调用 stash、不移动 frame。
- 日志标记 `mode=hitLayerContent`，同时记录 hit view 的 UIView alpha、layer opacity、交互状态和 frame。

HideDoubaoPiP v1.0.21

主要作用：修正 1.0.20 桌面仍可见的问题，同时保留 window 可见状态以维持 PiP 抢占能力。

主要变化：
- 在内部内容隐藏目标中加入 `PGHitTestExtendableView`，隐藏豆包 PiP 外层可见承载/命中层。
- 仍然不设置 `SBPictureInPictureWindow.alpha=0`，不调用系统 stash，不移动任何 view。
- 降低 layout 高频日志，只在隐藏状态变化或每个窗口低频采样时输出详细 target 状态。

HideDoubaoPiP v1.0.20

主要作用：不改变 `SBPictureInPictureWindow` 自身状态，只隐藏豆包 PiP 内部内容层，验证是否能保留和微信 PiP 的抢占能力。

主要变化：
- 不设置 window `alpha=0`，不禁用 window 触摸，不调用系统 stash，也不移动任何 PiP view。
- 仅将豆包 PiP 内部 `PGLayoutContainerView`、`PGControlsView`、`PGDimmingView` 等内容/控制层设为透明并禁用触摸。
- 日志标记 `mode=contentOnly`，记录隐藏前后的 window 状态和每个内部隐藏目标的 class、alpha、hidden、frame。

HideDoubaoPiP v1.0.19

主要作用：基于 1.0.2 的旧版视觉隐藏方式，移除系统 stash/边缘收纳调用，验证隐藏后是否仍保留 PiP 抢占能力。

主要变化：
- 保留豆包 PiP 识别、`SBPictureInPictureWindow` 层 `alpha=0`、禁用触摸和 `setAlpha:` 拦截。
- 删除 `setStashed` / `_setStashed` 调用路径，不移动 `PGHitTestExtendableView` 或其他 PiP 内部 view。
- 日志标记 `mode=alphaOnly`，用于区分不触发系统级边缘收纳的隐藏行为。

HideDoubaoPiP v1.0.18

主要作用：回到 1.0.2 旧版隐藏方式，并增加诊断日志定位“桌面仍可见”的原因。

主要变化：
- 保留旧版窗口层 `alpha=0`、禁用触摸和 `setAlpha:` 拦截逻辑。
- 增加 `[CALL]`、`[MISS]`、`[HIDE]`、`[SETALPHA-*]` 日志，记录每个 PiP window 的 bundle、alpha、hidden、关键 PG view frame、stash 目标和漏判原因。
- control 元数据增加 `Tag: role::enduser`，名称改为 `Hide Doubao PiP`，便于在 Sileo 已安装列表里显示和搜索。

HideDoubaoPiP v1.0.17

主要作用：按用户要求收敛为最小逻辑，只识别豆包 bundle 并移动豆包 PiP 到右侧边缘。

主要变化：
- 删除 viewTree 兜底识别、多 PiP 复杂判断、透明隐藏、stash 和手势模拟逻辑。
- 只通过 `com.bytedance.ios.doubaoime` 识别豆包 PiP；明确识别为其他 bundle 时直接跳过。
- 命中豆包后只移动 `PGHitTestExtendableView` 到右侧边缘，并记录 `[MOVE]` 日志。

HideDoubaoPiP v1.0.16

主要作用：修正诊断版本标识，继续使用 1.0.15 的 pan-only 拖拽手势筛选。

主要变化：
- 初始化日志更新为 `v1.0.16 simulated-drag-pan-only`，避免和 1.0.14 日志混淆。

HideDoubaoPiP v1.0.15

主要作用：修正 1.0.14 误命中 PiP 缩放手势的问题，继续定位真正拖拽手势。

主要变化：
- 查找拖拽手势时过滤 `Pinch`，只接受 `UIPanGestureRecognizer` 或类名包含 `Pan` / `Drag` 的手势。
- 当找不到拖拽手势时，记录目标 view 到父级 view 链上的全部 gesture class，便于定位系统真正的拖拽入口。

HideDoubaoPiP v1.0.14

主要作用：只模拟人手把豆包输入法 PiP 拖到右侧边缘，让系统自己执行边缘收纳。

主要变化：
- 不再使用窗口透明隐藏，不调用系统 stash 接口，不直接改 PiP 内部 view 的 frame。
- 识别豆包 PiP 后，找到拖拽命中层上的 pan/drag 手势，构造 `UITouch`，发送 began/moved/ended。
- 保留低频日志，记录手势目标、起点、终点和跳过原因，便于确认系统是否接收了拖拽。

HideDoubaoPiP v1.0.13

主要作用：回到旧版稳定的窗口层隐藏原理，解决纯拖动内部 view 后系统再次显示豆包浮窗的问题。

主要变化：
- 不再移动 PiP 内部 view，也不调用系统 stash 接口。
- 对识别出的豆包 PiP 在 `SBPictureInPictureWindow` 层设置 `alpha=0` 并禁用触摸。
- hook `setAlpha:`，系统后续尝试重新显示豆包 PiP 时继续压回透明，保留低频诊断日志。

HideDoubaoPiP v1.0.12

主要作用：验证豆包语音跳转是否由系统 stash 状态触发。

主要变化：
- 完全移除 `setStashed` / `_setStashed` 调用路径，只移动 `PGHitTestExtendableView` 到右侧边缘。
- 保留 1.0.11 的 PiP 窗口、bundle、active bundle 和 frame 诊断日志，并在日志中标记 `stashMode=disabled`。

HideDoubaoPiP v1.0.11

主要作用：保留 1.0.10 的停靠和 stash 行为，增加诊断日志用于确认豆包语音触发跳转的原因。

主要变化：
- 记录每次 PiP 触发时的窗口指针、识别 bundle、Pegasus 当前活跃 bundle 和可见 PiP 列表。
- 记录豆包停靠前后的拖拽层 frame，以及具体调用到哪些 `setStashed` / `_setStashed` 对象。

HideDoubaoPiP v1.0.10

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕右侧边缘，保留正常视频 PiP。

主要变化：
- 停靠动作改为移动 PiP 的拖拽命中层 `PGHitTestExtendableView`，不再移动内部内容容器。
- 同步调用 PiP controller 的系统 stash 接口，尽量接近人工拖到边缘后的收纳状态。

HideDoubaoPiP v1.0.9

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕右侧边缘，保留正常视频 PiP。

主要变化：
- 收敛为长期使用版本：只在明确识别到豆包输入法 bundle 后停靠。
- 删除递归扫描、详细测试日志、非豆包延迟重试和 viewTree 兜底执行。
- 固定按优先级选择一个 PiP 内容视图停靠到右侧边缘。

HideDoubaoPiP v1.0.8

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕边缘，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 命中豆包后递归扫描 PiP root 下所有可停靠的 `PG*` 小视图并推到右侧边缘。
- 多 PiP 分支也支持通过豆包 bundle 直接命中，不再只依赖 viewTree。

HideDoubaoPiP v1.0.7

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕边缘，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 将内容视图停靠方式从直接改 `frame` 改为叠加 `transform`，降低被系统 PiP layout 立即覆盖的概率。

HideDoubaoPiP v1.0.6

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕边缘，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 增加 PiP 创建后的延迟重试，处理豆包 bundle 和内容视图晚挂载。
- 命中豆包后直接将实际 `PG*` PiP 内容视图停靠到屏幕右侧边缘，不只依赖系统 stash 接口。

HideDoubaoPiP v1.0.5

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕边缘，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 增加 stash 目标类名和关键 `PG*` 视图 frame 诊断日志。
- 停靠时尝试 `_pipController`、adapter、Pegasus controller 和内部 PiP view controller，便于定位真正可拖拽对象。

HideDoubaoPiP v1.0.4

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕边缘，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 增加测试日志，记录 PiP 触发原因、识别来源、viewTree 匹配、多 PiP 状态和 stash 结果。
- 日志写入 `/var/mobile/Documents/PiPArrowHide.log`，并对 layout 高频触发做节流。

HideDoubaoPiP v1.0.3

主要作用：仅将豆包输入法创建的 PiP 悬浮窗自动停靠到屏幕边缘，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 行为从透明隐藏改为调用系统 PiP stash 接口自动停靠。
- 保留 bundle/process 和 viewTree 双重识别，继续只针对豆包输入法。
- 移除 `window.alpha = 0` 和禁用触摸逻辑。

HideDoubaoPiP v1.0.0

主要作用：仅隐藏豆包输入法创建的 PiP 悬浮窗，保留 Bilibili、微信等正常视频 PiP。

主要变化：
- 保留 v0.0.10 的 viewTree 共存识别逻辑，处理豆包与微信/Bilibili PiP 共存时的逐窗口识别。
- 移除 BKS、pid、idle timer、keepalive 等可能影响系统状态或耗电的逻辑。
- 减少高频诊断日志，只保留 INIT 和实际隐藏事件。
- HasMultipleActivePiPWindows 高频 layout 路径增加 100ms 缓存，setHidden/setAlpha/didMoveToWindow 关键事件强制刷新。
- WriteLog 改用 localtime_r + strftime 生成时间戳，避免每次创建 NSDateFormatter。
- 日志文件继续使用 /var/mobile/Documents/PiPArrowHide.log，最大 512KB，超过后截断重写，保留少量后续诊断日志。
